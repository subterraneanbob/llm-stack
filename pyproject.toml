[project]
name = "llm-stack"
version = "0.1.0"
description = "Двухсервисная система LLM-консультаций"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["auth_service"]

[tool.uv.sources]
auth_service = { workspace = true }

[tool.uv.workspace]
members = ["auth_service"]
